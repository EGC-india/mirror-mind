version: '3.8'

services:
  mirrormind:
    build: .
    image: ghcr.io/egc-india/mirrormind-app:latest
    container_name: mirrormind
    restart: always
    ports:
      - "3004:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
